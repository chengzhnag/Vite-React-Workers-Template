import React, { useState } from "react";
import {
  Form,
  Input,
  Button,
  Toast,
  Card
} from "antd-mobile";

const Entry = () => {
  return (
    <div>Entry</div>
  );
};

export default Entry;